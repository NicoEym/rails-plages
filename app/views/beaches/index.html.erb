<br>
  <div class="container">
    <% if policy(@beach).new? %>
      <%= link_to raw('<button type="button" class="btn btn-primary">Créer une nouvelle plage</button>'), new_beach_path(@beach) %>
    <% end %>
    <%= render 'shared/cardbeaches', beach: @beaches %>
  </div>
<br>
