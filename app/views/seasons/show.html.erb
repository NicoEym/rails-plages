<br>
  <div class="container">
    <h1 class="text-center">Planning</h1>
    <% if policy(@season).update? %>
        <%= link_to raw('<button type="button" class="btn btn-primary">Modifier les dates</button>'), edit_season_path(@season) %>
      <% end %>

    <%= render 'shared/cardsdate', dates: @dates %>
  </div>
<br>
