<div class="banner center-form" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://images.unsplash.com/photo-1464925257126-6450e871c667?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=800&q=60);">
      <br>
      <br>
  <div class="log-form col-xs-12 col-md-4">
    <h3>Chef de poste:</h3>
      <%= simple_form_for [@date, @beach, @team, @leader], remote: true, input_html: {  class: "form-lifeguard"} do |f| %>
          <%= f.select :user_id, options_from_collection_for_select(User.where(rank_id: @leader_rank.id), :id, :full_name), prompt: "Choisir",
                    required: true,
                    input_html: { autocomplete: "Perfil"} %>
        <% end %>
    <% @team_mates.each do |team_mate| %>
        <%= simple_form_for [@date, @beach, @team, team_mate], remote: true, input_html: {  class: "form-lifeguard"} do |f| %>
          <%= f.select :user_id, options_from_collection_for_select(User.where(rank_id: @team_mate_rank.id), :id, :full_name), prompt: "Choisir",
                    required: true,
                    input_html: { autocomplete: "Perfil"} %>
        <% end %>
    <% end %>
    <button id="submit-team" class="btn btn-lg btn-primary blue-nav col-xs-9 col-md-4" style="margin-bottom: 0.25em"><i class="fas fa-check"></i> Modifier l'équipe</button>
    <br>
</div>
</div>
